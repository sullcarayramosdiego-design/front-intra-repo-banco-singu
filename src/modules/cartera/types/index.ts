export interface CarteraActivaItem {
  tipo_producto: string;
  region: string;
  saldo_total: number;
  total_cuentas: number;
}

export interface ClienteComposicionItem {
  tipo_persona: string;
  segmento: string;
  ciudad: string;
  cantidad_clientes: number;
}
